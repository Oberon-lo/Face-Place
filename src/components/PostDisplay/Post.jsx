import React from 'react';
import Comments from './Comments';

const Post = (props) => {
    return (
        <div>
            Post
            <Comments/>
        </div>
    );
};

export default Post;